.DS_Store

# SwiftPM
.build/
.swiftpm/
Package.resolved

# Vendored, locally-patched SwiftTerm copy used by the Step 0 spike.
# Recreation is documented in spike/README.md — the patch is one exclude line.
spike/vendor/
