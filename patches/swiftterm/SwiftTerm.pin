# SwiftTerm vendor pin — read by app/Scripts/verify-vendor.sh
#
# WHY THIS FILE EXISTS
# vendor/ is gitignored (.gitignore:13), vendor/SwiftTerm is not itself a git repo,
# app/Package.swift:20 is an unpinned `.package(path:)`, and Package.resolved is
# ignored too (.gitignore:6). So before this pin existed, NOTHING in the repository
# recorded which upstream revision was vendored or that a local patch had been
# applied to it. The failure was asymmetric and that is the whole problem:
#
#   * a MISSING vendor/ fails loudly  — SwiftPM cannot resolve the path dependency
#   * a RE-VENDORED, UNPATCHED tree fails SILENTLY — it compiles and runs fine,
#     because the patch only removes a build-time resource
#
# So following app/README.md's recreation steps faithfully but forgetting a patch
# produced a green build with a different dependency than the one that was tested.
# verify-vendor.sh turns that silent case into a loud one.
#
# As of patch 0002 (Buffer.swift, the #494 reflow fix) the stakes are higher: a tree
# missing 0002 does not merely build differently, it silently CORRUPTS SCROLLBACK when a
# window is narrowed. That is why the Buffer.swift check below is a hard failure.
#
# VERIFIED PROVENANCE (2026-07-27, amended 2026-07-29)
# A full recursive diff of the vendored tree against the upstream v1.15.0 tarball
# reported exactly ONE differing entry: Package.swift. Everything else — all 190+
# source files including Buffer.swift and Terminal.swift — was byte-identical to
# upstream.
#
# As of 2026-08-25 the tree differs from upstream in SIX places — Package.swift,
# Buffer.swift, EscapeSequenceParser.swift, MacTerminalView.swift and
# AppleTerminalView.swift are modified, and PtmuxDcsHandler.swift is added new by
# 0005 — carrying EIGHT patches between them: 0001 on Package.swift, 0002/0003/0004
# on Buffer.swift, 0005 on EscapeSequenceParser.swift (plus the new file), 0006 and
# 0008 on MacTerminalView.swift, and 0007 on AppleTerminalView.swift.
# File count and patch count are not the same number and have not been since 0003;
# do not infer one from the other.
#
# Regenerate the hashes below with:
#   shasum -a 256 vendor/SwiftTerm/Package.swift
#   shasum -a 256 vendor/SwiftTerm/Sources/SwiftTerm/Buffer.swift
#   shasum -a 256 vendor/SwiftTerm/Sources/SwiftTerm/Terminal.swift
#   shasum -a 256 vendor/SwiftTerm/Sources/SwiftTerm/EscapeSequenceParser.swift
#   shasum -a 256 vendor/SwiftTerm/Sources/SwiftTerm/Mac/MacTerminalView.swift
#   shasum -a 256 vendor/SwiftTerm/Sources/SwiftTerm/Apple/AppleTerminalView.swift

upstream_repo   = https://github.com/migueldeicaza/SwiftTerm
upstream_tag    = v1.15.0
upstream_commit = dd2fb8ac5b861e7bf617c872895e338f38165648

# Package.swift AFTER applying 0001-ship-metal-shader-as-copy-resource.patch.
# This is the hash verify-vendor.sh requires.
#
# What 0001 does, and what it used to do wrong. Upstream declares the Metal renderer's
# shader as `.process("Apple/Metal/Shaders.metal")`, which invokes the offline Metal
# compiler at build time. That is impossible here — Xcode 26.6 moved the toolchain to
# `xcodebuild -downloadComponent MetalToolchain` and this machine has Command Line Tools
# only — and it fails LOUDLY: `error: unable to spawn process 'metal'`.
#
# Until 2026-07-31 this patch avoided that with `exclude:`, which also removed the shader
# from the resource bundle. That was one step too far. MetalTerminalRenderer.makeLibrary
# (Apple/Metal/MetalTerminalRenderer.swift:2746) falls back to compiling the .metal SOURCE
# at RUNTIME (:2765) — no offline toolchain needed — but only if the file is in the bundle.
# With `exclude` it was not, so the fallback threw `shaderSourceMissing` (:2762) and the
# vendored GPU renderer was unreachable for the life of the project, silently.
#
# 0001 is now `.copy`, which ships the raw source and never invokes the build-time
# compiler. Both halves measured rather than assumed: `.process` fails as quoted above,
# `.copy` builds and lands the file at SwiftTerm_SwiftTerm.bundle/Contents/Resources/,
# and runtime compilation of the real shaders on an M4 Pro takes 173ms cold / ~0ms warm
# with all seven required functions present. app/Scripts/check-metal-renderer.sh is the
# gate; .afk/research/performance-audit-2026-07-31.md §1 is the argument.
patched_package_swift  = 8cc7331ee1f5168eb686fd147987ea868d3e0f4c6c2f65307c79f78afe3003c9

# Package.swift as shipped upstream at v1.15.0. Matching THIS hash means the tree was
# re-vendored and 0001 was never applied. Note this one case is NOT silent: with upstream's
# `.process` the build dies at `unable to spawn process 'metal'`. verify-vendor.sh still
# reports it by name, because a named cause beats a compiler error about a missing tool.
upstream_package_swift = a1dfb1317409a108ad5c9d8db83e5d058bdbbd700ecbf8892f27a498ac200575

# Buffer.swift AFTER applying ALL THREE of 0002-index-iswrapped-buffer-absolute.patch,
# 0003-trim-lines-on-narrowing-for-all-buffers.patch and
# 0004-gate-resize-post-condition-behind-debug.patch. One hash covers all three: they are
# independent hunks of the same file (0002 near line 1170, 0003 near line 520, 0004 near
# line 544), and a single hash is deliberately unforgiving — a tree carrying only some of
# them matches neither this nor the upstream hash and verify-vendor.sh calls it "unknown"
# (exit 3). This is the hash verify-vendor.sh requires.
#
# That combined hash is also why 0004 needs no check of its own even though, unlike 0002
# and 0003, its absence corrupts nothing. A tree missing it is merely slower and carries a
# release-build `abort()`; it still cannot pass, because it cannot match this hash. The
# existing "half-patched is not a state this project supports" rule covers it for free.
#
# This file used to be pinned as a version TRIPWIRE with the note "it is unmodified
# upstream and must stay that way while option A is unchosen". That is no longer true,
# and the sentence was removed rather than softened: Buffer.swift now carries REQUIRED
# patches, so this is a patch check exactly like Package.swift above, not a tripwire.
#
# What 0002 fixes — the reflow defect of record: upstream lines 1171 and 1211 set
# `_lines[_y].isWrapped = true` using a SCREEN-relative index, while the adjacent content
# writes at 1176 and 1224 correctly use the BUFFER-absolute `_lines[_y + _yBase]` (`_y` is
# screen-relative per Buffer.swift:22, 40-43). With non-empty scrollback the wrapped-line
# flag therefore landed `_yBase` rows off target and reflow mangled history. See
# .afk/plans/emulator-foundation-probe-and-vendor-integrity.md §2 and SwiftTerm issue #494.
#
# What 0003 fixes — the ALT-buffer resize defect: upstream keeps the "trim each line to
# the new width" loop inside `if isReflowEnabled`, and `isReflowEnabled` is just
# `hasScrollback` (Buffer.swift:419), which is false for the alt buffer (built
# `scrollback: nil`, Terminal.swift:694). Narrowing therefore shrank `cols` while every
# BufferLine kept its old width, and the next WIDENING copied those stale cells back into
# the visible grid (BufferLine.swift:197). Under tmux — which lives in the alt buffer and
# repaints only deltas — that is content bleeding across pane and window boundaries.
# See .afk/research/tmux-bleed-altbuffer-resize-2026-07-29.md.
#
# What 0004 fixes — a debug assertion that ships. Upstream wraps this file's other five
# debug blocks in `#if DEBUG` (:47, :62, :81, :98, :116) but leaves the `// DEBUG:
# Post-condition` loop after the resize trim ungated. Verified against the pristine
# v1.15.0 file, where it sits at :533-546 with no surrounding conditional — so this is an
# upstream leftover, not something 0002/0003 introduced. It walks the viewport PLUS THE
# ENTIRE SCROLLBACK on every resize (the third such walk in the function, after reflow and
# after 0003's trim) and calls `abort()` on mismatch. Every size change reaches it:
# processSizeChange (live window drag — once per drag frame — plus ⌘B and full screen) and
# resetFont (⌘+/⌘-/⌘0, ⌘R). At Umber's default 1000-line scrollback that is 1000 iterations
# per buffer per resize event, and a hard crash path raised from inside a window drag in a
# terminal whose whole job is hosting long-running agent sessions.
# See .afk/research/performance-audit-2026-07-31.md §2.
#
# 0002 and 0003 are verified behaviourally, and each gate was validated by falsification:
# app/Scripts/check-reflow.sh FAILS 6 of 8 cases without 0002, and
# app/Scripts/check-altbuffer-resize.sh FAILS 2 of 4 without 0003 (cases 1 and 2, while
# its control and its render-range case keep passing). If you re-vendor, run both.
#
# 0004 gets NO behavioural gate, deliberately, and this is the honest reason: it changes no
# release behaviour that any check could observe. Its whole effect is deleting work and
# deleting a crash path, so a passing gate would prove only that the compiler honours
# `#if DEBUG`. What guards it instead is this pin's hash plus the fact that both remaining
# Buffer.swift gates still pass with it applied — i.e. that removing the assertion did not
# remove anything the assertion was catching. Both were re-run when 0004 landed.
patched_buffer_swift  = 05d64bab59de389334e0557b7cbf776551d0cf3d9bf9ab4d59df09736d31c8d1

# Buffer.swift as shipped upstream at v1.15.0. Matching THIS hash means the tree was
# re-vendored and NEITHER 0002 nor 0003 was applied — the silent-failure case, and now the
# WORSE of the two files: patch 0001 only drops a build-time resource, but a Buffer.swift
# missing its patches compiles, runs, and silently corrupts the buffer — 0002's absence
# mangles scrollback on every window narrowing, 0003's bleeds stale cells across tmux panes
# on every widening. verify-vendor.sh reports it by name and FAILS (it only warned while
# Buffer.swift was unmodified upstream).
upstream_buffer_swift = 7b65f2b9ce21c95b124848b76856e727fd8a407cb7354a1dc19ab82f4d2c16bd

# Terminal.swift is still a pure version tripwire — unmodified upstream, no patch. The
# reflow read-back API check-reflow.sh depends on lives here
# (getBufferAsData, Terminal.swift:5947), so a change means re-checking that gate.
terminal_swift = 7d29fe260ff9561ac827e3e145838005c1ad5306f5ca0d087fec2b813520ede9
# EscapeSequenceParser.swift AFTER applying 0005-add-dcs-ptmux-passthrough.patch.
# This is the hash verify-vendor.sh requires.
#
# What 0005 does: adds a PtmuxDcsHandler branch to dispatchDcs() for DCS sequences
# with an empty collect and final-byte 't' (0x74). The DCS state machine already
# handles ESC P -> dcsEntry and dispatches 't' as the final-byte with dcsHook,
# making collect=[], code=0x74 the correct registration key. The handler buffers
# all put() bytes, validates the "mux;" prefix on unhook(), un-doubles ESC pairs
# (tmux's wire encoding), and re-feeds the inner sequence to terminal.feed().
#
# Severity: SILENT. A tree missing 0005 compiles, runs, and silently drops every
# DCS Ptmux sequence — meaning OSC 52 clipboard, graphics, and custom sequences
# from apps like neovim running inside tmux with allow-passthrough=on are lost.
# There is no runtime error, no log entry, just a missing feature.
#
# The patch also adds PtmuxDcsHandler.swift (a new file). That file has no
# upstream counterpart, so its absence means the build would fail (undefined
# type) — making 0005 louder than 0004 about its own absence: missing the file
# is a compile error, missing just the EscapeSequenceParser.swift change is silent.
#
# No behavioural gate script exists for 0005 (no tmux available in the build
# environment). The hash pair below is the machine-verifiable equivalent: a tree
# with the handler registered but not the file fails to compile; a tree with the
# file but not the registration silently drops passthrough sequences and hits this
# unknown-hash branch.
#
# Regenerate with:
#   shasum -a 256 vendor/SwiftTerm/Sources/SwiftTerm/EscapeSequenceParser.swift
#   # then update patched_escape_seq_parser below
patched_escape_seq_parser  = 5f1a11eb0d2523e4bd92b057f7293a3f1fa0f3f2be86efe714a9616668bd46ae

# EscapeSequenceParser.swift as shipped upstream at v1.15.0.
upstream_escape_seq_parser = ee651a9ffaa1adaaa106d89121c17903227d3c4a38312c42a3aa2f34058e937a

# PtmuxDcsHandler.swift — added by 0005, has no upstream counterpart.
# This is the hash verify-vendor.sh requires. A missing file is a compile error,
# but a modified file would pass the existence check silently without this pin.
ptmux_dcs_handler = 78c20efe0b206162b6a771fa2f089474aeba2102508e29f1b83efcf293492b89

# MacTerminalView.swift AFTER applying 0006-gate-linefeed-selection-clear-on-mouse-mode.patch.
#
# What 0006 fixes: upstream's linefeed(source:) callback clears the terminal
# selection on every LF byte when `allowMouseReporting` is true — which it is by
# default. Every OTHER allowMouseReporting guard in the patched file (mouseDown
# :2472, mouseUp :2520, mouseDragged :2537, scrollWheel :2771) also checks
# `terminal.mouseMode` — nested one level inside for mouseDragged — but linefeed
# was the sole site that omitted it.
# The effect: at a plain shell prompt (mouseMode == .off), every newline in the
# output clears the selection before ⌘C can copy it.
#
# Severity: SILENT. A tree missing 0006 compiles, runs, and silently clears the
# user's selection during normal output.
#
# Regenerate with:
#   shasum -a 256 vendor/SwiftTerm/Sources/SwiftTerm/Mac/MacTerminalView.swift
patched_mac_terminal_view  = 0dc5cab8ebad153f5808d4fb209afc8bba1a5288935c2b7c355a4e35d7f773ea

# MacTerminalView.swift as shipped upstream at v1.15.0.
upstream_mac_terminal_view = 2adfd7861a746d56e8e88109fea6a96f4ee7e17972dac60417864168cded4041

# AppleTerminalView.swift AFTER applying
# 0007-gate-feedprepare-selection-clear-on-mouse-mode.patch.
#
# What 0007 fixes: upstream's feedPrepare() callback clears the terminal selection
# on every chunk of pty output when `allowMouseReporting` is true — which it is by
# default. This is the same defect pattern 0006 fixed in linefeed
# (MacTerminalView.swift:888): `allowMouseReporting` is a CAPABILITY flag (can this
# terminal report mouse events?) not an ACTIVE-SESSION flag (is a program actually
# tracking the mouse?). The correct check is `terminal.mouseMode != .off`.
#
# The effect: at a plain shell prompt (mouseMode == .off), ANY pty output between
# the user finishing their selection and pressing ⌘C clears selection.active, which
# causes validateUserInterfaceItem to disable Copy, silently swallowing the keystroke.
# A shell prompt redraw, a cursor position update, or any background output is enough.
#
# Severity: SILENT. A tree missing 0007 compiles, runs, and silently prevents
# copy/paste — the selection highlight may even remain visible while the underlying
# `selection.active` flag has already been cleared.
#
# Regenerate with:
#   shasum -a 256 vendor/SwiftTerm/Sources/SwiftTerm/Apple/AppleTerminalView.swift
patched_apple_terminal_view  = d08379316e47745b9aeb0b1f6fbede03cedc4441e36685f84638d900207ca786

# AppleTerminalView.swift as shipped upstream at v1.15.0.
upstream_apple_terminal_view = d28360034441f9a4573debcfe8ac5f86b3c57b3059da5f94ba2549838714e45e
